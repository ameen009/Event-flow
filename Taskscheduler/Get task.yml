info:
  name: Get task
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/tasks/1
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
