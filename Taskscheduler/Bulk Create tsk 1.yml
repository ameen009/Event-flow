info:
  name: Bulk Create tsk 1
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/tasks/bulk-execute
  body:
    type: json
    data: |-
      {
        "taskIds": [1,2,3]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
