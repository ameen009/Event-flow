# JWT secret — minimum 64 characters for HMAC-SHA512
# Generate a strong secret: openssl rand -base64 64
JWT_SECRET=change-this-to-a-very-long-secret-key-minimum-64-characters-for-hmac-sha512-algorithm
